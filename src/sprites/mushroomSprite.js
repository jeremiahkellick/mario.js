import { npcsAndItemsImage } from '../files';
import Vector from '../Vector';

export default {
  image: npcsAndItemsImage,
  frames: {
    main: { position: new Vector(156, 121), size: new Vector(16, 16) }
  }
};
